# Agent Run 基础记录 Service TDD 证据

> 来源计划：[MVP 执行清单](../03.plan/02.MVP执行清单.md)
> 分析记录：[Agent Run 基础记录 Service](../05.analysis/2026-07-17-agent-run-service.md)

## 用户旅程

作为会话编排层，我希望用用户和幂等键创建或复用 Agent Run，以便客户端重试不会重复执行模型调用。

作为会话编排层，我希望把运行收敛为成功、失败、超时或取消，以便保留可追溯的执行结果和脱敏错误。

## RED / GREEN 记录

- RED：`pnpm test -- --runInBand agent-runs.e2e-spec.ts` — 失败；测试入口编译通过，但因 `AgentRunsService` 尚不存在而报模块缺失。
- RED 检查点：`ad80d0b test: 补充 Agent Run 基础记录失败用例`。
- GREEN：`pnpm test -- --runInBand agent-runs.e2e-spec.ts` — 通过，7 个测试。
- 安全边界 RED：新增 Authorization 错误摘要用例后测试先因未拦截而失败。
- 安全边界 GREEN：修正敏感错误摘要校验后相关测试通过。
- GREEN 检查点：`4fd19f4 feat: 实现 Agent Run 基础记录服务`；安全修复检查点：`0546332 fix: 拒绝 Agent Run 敏感错误摘要`。

## 测试规格

| # | 保证内容 | 测试 | 类型 | 结果 |
|---|---|---|---|---|
| 1 | 相同用户和幂等键只返回一条 running Run | `agent-runs.e2e-spec.ts` | Service 单元 | PASS |
| 2 | 数据库唯一冲突时复用并发创建的已有 Run | `agent-runs.e2e-spec.ts` | Service 单元 | PASS |
| 3 | 成功、失败、超时和取消均写入完成时间和合法终态 | `agent-runs.e2e-spec.ts` | Service 单元 | PASS |
| 4 | 终态 Run 不允许再次更新，且用户不能访问他人 Run | `agent-runs.e2e-spec.ts` | Service 单元 | PASS |
| 5 | 快照最多保存三张知识卡且拒绝敏感字段 | `agent-runs.e2e-spec.ts` | Service 单元 | PASS |
| 6 | Token、成本和耗时拒绝负数或非法值 | `agent-runs.e2e-spec.ts` | Service 单元 | PASS |
| 7 | 错误摘要拒绝 Authorization、敏感诊断和过长内容 | `agent-runs.e2e-spec.ts` | Service 单元 | PASS |

## 全量验证和覆盖率

- `pnpm test -- --runInBand` — 通过，10 个测试套件、64 个测试。
- `pnpm run typecheck` — 通过。
- `pnpm run build` — 通过。
- `pnpm run test:coverage` — 通过：语句 96.87%、分支 82.96%、函数 97.09%、行 97.58%。

## 已知缺口

- 尚未接入真实会话、消息、模型调用和 SSE；这些属于阶段 3 Agent 核心闭环。
- 尚未执行真实 PostgreSQL 并发幂等和外键集成测试。
