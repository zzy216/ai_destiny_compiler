# 基础工程与模型协议 TDD 证据

> 执行日期：2026-07-16  
> 源计划：[MVP 下一步实施计划](../03.plan/01.MVP下一步实施计划.md)  
> 执行清单：[MVP 执行清单](../03.plan/02.MVP执行清单.md)

## 已完成用户旅程

1. 作为 API 运维者，我希望错误的环境配置在启动前失败，以免服务带着不安全配置运行。
2. 作为 API 运维者，我希望通过健康检查区分 API 正常、数据库正常和数据库不可用。
3. 作为开发者，我希望请求日志为结构化 JSON 且不含凭据，便于排查请求耗时和状态。
4. 作为模型调用 Service，我希望使用 AES-256-GCM 保存 API Key，并能通过协议适配器调用 OpenAI-compatible 或 Ollama 模型。

## RED / GREEN 证据

- 基础工程 RED：新增测试首次执行因 `src/config/environment` 不存在而失败。
- 基础工程 GREEN：`foundation.e2e-spec.ts` 通过，环境校验和 `/api/health` 已实现。
- 日志 RED：新增测试首次执行因 `src/common/request-logger` 不存在而失败。
- 日志 GREEN：`request-logger.e2e-spec.ts` 通过，Pino middleware 已接入全局应用。
- 加密 RED：新增测试首次执行因 `src/models/model-credential-cipher` 不存在而失败。
- 加密 GREEN：`credential-encryption.e2e-spec.ts` 通过，32 字节 Base64 主密钥、随机 IV、认证标签和密钥版本均受验证。
- 适配器 RED：新增测试首次执行因 `src/models/model-adapters` 不存在而失败。
- 适配器 GREEN：`model-adapters.e2e-spec.ts` 通过，OpenAI-compatible 与 Ollama 协议层均完成安全错误归一化。

## 测试规格

| # | 保证 | 测试文件或命令 | 类型 | 结果 |
| --- | --- | --- | --- | --- |
| 1 | 环境配置可标准化，缺失密码或非法端口会失败 | `test/foundation.e2e-spec.ts` | 集成/边界 | PASS |
| 2 | `/api/health` 在数据库关闭时返回 `database=disabled` | `test/foundation.e2e-spec.ts` | E2E | PASS |
| 3 | 请求日志 middleware 可创建并输出固定字段结构 | `test/request-logger.e2e-spec.ts` | 单元 | PASS |
| 4 | 凭据使用 AES-256-GCM 加密，篡改后无法解密 | `test/credential-encryption.e2e-spec.ts` | 安全/单元 | PASS |
| 5 | OpenAI-compatible 和 Ollama 响应统一为内部结果格式 | `test/model-adapters.e2e-spec.ts` | 协议集成 | PASS |
| 6 | Provider HTTP、超时和网络错误不会回显原始敏感错误 | `test/model-adapters.e2e-spec.ts` | 安全/边界 | PASS |
| 7 | 全量测试、类型检查、构建和 OpenAPI 生成通过 | `pnpm test`, `pnpm test:coverage`, `pnpm typecheck`, `pnpm build`, `pnpm generate:openapi` | 验收 | PASS |

## 覆盖率与已知缺口

```text
Test Suites: 6 passed, 6 total
Tests:       29 passed, 29 total
Statements:  97.1%
Branches:    80.92%
Functions:   98.78%
Lines:       97.14%
```

尚未完成的内容：种子数据、模型连接测试业务 Service、模型 CRUD/发布/停用/默认模型事务，以及 Controller 对真实 Service 的接入。当前适配器只完成协议层，不能作为阶段 2 全部验收通过的依据。
