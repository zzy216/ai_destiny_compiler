# 首批种子数据 TDD 证据

> 执行日期：2026-07-17
> 源计划：[MVP 下一步实施计划](../03.plan/01.MVP下一步实施计划.md)
> 执行清单：[MVP 执行清单](../03.plan/02.MVP执行清单.md)
> 分析记录：[首批种子数据分析](../05.analysis/2026-07-17-seed-initial-data.md)

## 用户旅程

1. 作为开发者，我希望通过显式命令创建基础数据，以便在本地直接验证业务关系。
2. 作为安全维护者，我希望种子密码只从环境变量读取并保存为 Argon2id 哈希，以免凭据进入仓库或日志。
3. 作为开发者，我希望重复执行种子命令不会重复创建固定数据，也不会覆盖已有账号密码。

## RED / GREEN 证据

- RED：新增 `seed-data.e2e-spec.ts` 首次执行因 `src/database/seed` 不存在而失败。
- GREEN：新增种子执行器后，3 个种子测试通过；重复执行使用内存仓储验证记录数量保持不变。
- 安全边界：以生产环境和数据库关闭配置执行 `pnpm seed`，命令在连接数据库前拒绝自动种子。

## 测试规格

| # | 保证 | 测试文件或命令 | 类型 | 结果 |
| --- | --- | --- | --- | --- |
| 1 | 生产环境、无效环境和缺少/过短密码会拒绝种子 | `test/seed-data.e2e-spec.ts` | 单元/边界 | PASS |
| 2 | 种子定义包含 2 个用户、2 个模型、1 个教练配置和 12 张唯一知识卡 | `test/seed-data.e2e-spec.ts` | 单元 | PASS |
| 3 | 重复执行不增加记录，密码以 `$argon2id$` 开头的哈希保存 | `test/seed-data.e2e-spec.ts` | 集成模拟/安全 | PASS |
| 4 | 全量测试通过 | `pnpm test` | 集成 | PASS |
| 5 | 类型检查和构建通过 | `pnpm typecheck`、`pnpm build` | 验收 | PASS |
| 6 | 覆盖率满足 80% 门槛 | `pnpm test:coverage` | 覆盖率 | PASS |

## 覆盖率与已知缺口

```text
Test Suites: 7 passed, 7 total
Tests:       32 passed, 32 total
Statements:  95.9%
Branches:    80.26%
Functions:   96.87%
Lines:       95.85%
```

当前测试使用内存仓储验证事务调用和幂等逻辑；真实 PostgreSQL 数据库中的种子执行仍需在本地配置数据库和种子密码后运行 `pnpm seed` 验证。生产环境不会自动创建默认账号。
