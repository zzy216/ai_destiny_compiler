# 阶段 8 测试和发布 TDD 证据

> 分析记录：[阶段 8 测试和发布](../05.analysis/2026-07-19-phase-8-release.md)

## 用户旅程

1. 作为维护者，我希望在内测发布前执行一个统一脚本，以便一次性验证 API、管理后台和 Flutter 客户端。
2. 作为维护者，我希望 OpenAPI 明确标记受保护接口，以便客户端和接口调试工具不会误判匿名可调用。
3. 作为维护者，我希望发布清单覆盖迁移、种子数据、备份和回滚，以便内测发布可重复、可追溯。

## RED / GREEN 检查点

- RED：新增 `scripts/verify-release.sh` 前，发布前检查散落在各子项目目录，缺少统一验收入口。
- RED：加强 `apps/api/test/api-contract.e2e-spec.ts` 前，契约测试没有整体检查受保护 OpenAPI 操作是否声明 bearer auth。
- GREEN：阶段 8 完成后，统一脚本串联 API 覆盖率、类型检查、构建、OpenAPI 生成、管理后台覆盖率、类型检查、构建、Flutter 测试和静态分析。

## 测试规格

| # | 保证内容 | 测试文件/命令 | 类型 | 结果 |
|---|---|---|---|---|
| 1 | API 文档端点设置基础安全响应头 | `apps/api/test/api-contract.e2e-spec.ts` | E2E | PASS |
| 2 | 受保护 OpenAPI 操作声明 bearer auth | `apps/api/test/api-contract.e2e-spec.ts` | E2E | PASS |
| 3 | 公开响应 schema 不暴露存储态密钥、哈希和密文字段 | `apps/api/test/api-contract.e2e-spec.ts` | E2E | PASS |
| 4 | API、管理后台和 Flutter 可通过发布前统一验收脚本 | `scripts/verify-release.sh` | Release check | PASS |

## 验证结果

- `cd apps/api && pnpm test -- api-contract.e2e-spec.ts` — 通过；1 个测试套件、14 个测试通过。
- `./scripts/verify-release.sh` — 通过；API 18 个测试套件、120 个测试通过；管理后台 7 个测试文件、8 个测试通过；Flutter 4 个测试通过；API/管理后台类型检查和构建、OpenAPI 生成、Flutter analyze、`git diff --check` 均通过。
- `git diff --check` — 已由统一验收脚本执行并通过。

## 已知缺口

- 未接入外部 CI、真实部署环境或应用商店发布流程；本阶段只固定仓库内内测发布准备。
- Apifox 导入需要在外部工具中手动执行，仓库侧以 `apps/api/openapi.json` 和发布清单作为可重复输入。
