# 阶段 4 React 管理后台 TDD 证据

> 分析记录：[阶段 4 React 管理后台](../05.analysis/2026-07-18-phase-4-react-admin.md)

## 用户旅程

1. 作为管理员，我希望在后台查看和维护系统模型，以便不修改代码即可发布、停用和切换默认模型。
2. 作为管理员，我希望编辑并发布教练配置和法典知识卡，以便新的 Agent 执行使用可追溯的配置版本。
3. 作为管理员，我希望查看 Agent Run 的状态、模型和耗时摘要，以便定位执行问题，同时不暴露凭据和完整敏感结果。

## RED / GREEN 检查点

- RED：`apps/admin/pnpm test` — 5 个测试套件因目标页面和 API Client 尚未实现而失败；检查点提交 `5ef1c28`（`test: 补充阶段4管理后台失败用例`）。
- GREEN：`apps/admin/pnpm test` — 6 个测试文件、7 个测试通过。
- GREEN：`apps/api/pnpm test` — 15 个测试套件、83 个测试通过。

## 测试规格

| # | 保证内容 | 测试文件/命令 | 类型 | 结果 |
|---|---|---|---|---|
| 1 | 后台壳显示导航、开发模式和运营总览 | `apps/admin/src/app/App.test.tsx` | React 组件 | PASS |
| 2 | 模型列表显示状态和脱敏凭据，不渲染 API Key | `apps/admin/src/features/models/ModelsPage.test.tsx` | React 组件 | PASS |
| 3 | 教练配置显示版本、目标和编辑入口 | `apps/admin/src/features/coach-config/CoachConfigPage.test.tsx` | React 组件 | PASS |
| 4 | 知识卡显示名称、分类、标签和发布状态 | `apps/admin/src/features/knowledge-cards/KnowledgeCardsPage.test.tsx` | React 组件 | PASS |
| 5 | Agent Run 只显示模型、状态、耗时和 Token 摘要 | `apps/admin/src/features/agent-runs/AgentRunsPage.test.tsx` | React 组件 | PASS |
| 6 | 管理服务提供分页、发布和安全摘要 | `apps/api/test/admin-management.spec.ts` | Service 单元 | PASS |
| 7 | 管理 Controller 转发列表和发布操作 | `apps/api/test/admin-management-controller.spec.ts` | Controller 单元 | PASS |
| 8 | 生产环境不会暴露开发期固定管理员接口 | `apps/api/test/development-admin.guard.spec.ts` | 安全单元 | PASS |
| 9 | OpenAPI 包含阶段 4 管理端点 | `apps/api/test/api-contract.e2e-spec.ts` | API 契约 | PASS |

## 验证结果

- `cd apps/admin && pnpm typecheck` — 通过。
- `cd apps/admin && pnpm build` — 通过；Vite 有单 chunk 大于 500 kB 的性能提示，留待后续路由懒加载优化。
- `cd apps/admin && pnpm test:coverage` — 通过；语句/行覆盖率 81.45%。
- `cd apps/api && pnpm typecheck` — 通过。
- `cd apps/api && pnpm build` — 通过。
- `cd apps/api && pnpm test` — 通过；15 个测试套件、83 个测试通过。
- `cd apps/api && NODE_ENV=test DATABASE_ENABLED=false pnpm generate:openapi` — 通过，已更新 `apps/api/openapi.json`。
- `git diff --check` — 通过。

## 已知缺口

- 尚未接入真实管理员登录、JWT、角色守卫、CSRF、限流和审计；新管理端点在生产环境由开发保护守卫阻断，阶段 5 替换为真实认证。
- 尚未配置 Playwright 浏览器 E2E；当前以 React 组件测试、API Service/Controller 测试、OpenAPI 契约测试和生产构建验证为主。
- Ant Design 生产包仍有大 chunk 警告，后续可按路由拆分。
