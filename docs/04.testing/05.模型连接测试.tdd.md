# 模型连接测试 TDD 证据

> 来源计划：[MVP 执行清单](../03.plan/02.MVP执行清单.md)

## 用户旅程

作为管理员或自定义模型用户，我希望测试已保存模型的连接，以便在开始会话前知道模型是否可达。

## RED / GREEN 记录

- RED：`pnpm test -- models-service.e2e-spec.ts` — 失败，目标测试因 `ModelsService` 尚不存在而无法编译；修正测试辅助类型后，失败原因仅保留为缺少目标实现。
- GREEN：`pnpm test -- models-service.e2e-spec.ts` — 通过，5 个测试全部通过。
- 回归：`pnpm test` — 通过，8 个测试套件、37 个测试全部通过。

## 测试规格

| # | 保证内容 | 测试 | 类型 | 结果 |
|---|---|---|---|---|
| 1 | 使用当前草稿版本并在调用适配器前解密凭据 | `models-service.e2e-spec.ts` | Service 集成 | PASS |
| 2 | 没有草稿时回退到已发布版本 | `models-service.e2e-spec.ts` | Service 集成 | PASS |
| 3 | 不支持的协议不发起网络请求；不存在的模型返回 NotFound | `models-service.e2e-spec.ts` | Service 单元 | PASS |
| 4 | 供应商 HTTP 错误被归一化且不包含原始错误体 | `models-service.e2e-spec.ts` | Service 集成 | PASS |
| 5 | 数据库关闭的契约测试仍保持 `501` 占位行为 | `api-contract.e2e-spec.ts` | API E2E | PASS |

## 覆盖率和已知缺口

`pnpm test:coverage` — 通过：语句 95.63%、分支 80.72%、函数 96.07%、行 95.55%。模型 CRUD、发布/停用/默认模型、认证授权、用户归属校验及自定义 Base URL 的 SSRF 防护仍未在本批次实现。

## 检查点提交

- RED：`253bdc8 test: add model connection service coverage`
- GREEN：`d90503a feat: wire model connection test service`
