# 模型管理底座 TDD 证据

> 来源计划：[MVP 执行清单](../03.plan/02.MVP执行清单.md)
> 分析记录：[阶段 2 模型管理底座](../05.analysis/2026-07-17-model-management-lifecycle.md)

## 用户旅程

作为管理员，我希望创建、修改、发布、停用和设置默认系统模型，以便控制新会话可用的模型。

作为用户，我希望管理自己的自定义模型并测试连接，以便在咨询前使用可用的模型配置。

## RED / GREEN 记录

- RED：`pnpm test -- --runInBand models-lifecycle.e2e-spec.ts` — 失败；测试夹具编译通过后，剩余失败均来自 `ModelsService` 缺少生命周期方法。
- RED 检查点：`39ca0dc 添加模型管理生命周期失败用例`。
- GREEN：`pnpm test -- --runInBand` — 通过，9 个测试套件、57 个测试全部通过。
- 类型检查：`pnpm run typecheck` — 通过。
- 覆盖率：`pnpm run test:coverage` — 通过，语句 96.69%、分支 81.72%、函数 96.73%、行 97.43%，满足 80% 门槛。

## 测试规格

| # | 保证内容 | 测试 | 类型 | 结果 |
|---|---|---|---|---|
| 1 | 创建系统模型时生成草稿版本并加密 API Key | `models-lifecycle.e2e-spec.ts` | Service 单元 | PASS |
| 2 | 更新模型创建新版本，并将旧草稿标记为 superseded | `models-lifecycle.e2e-spec.ts` | Service 单元 | PASS |
| 3 | 发布模型时切换版本指针并保留旧版本 | `models-lifecycle.e2e-spec.ts` | Service 单元 | PASS |
| 4 | 仅已发布且可选的系统模型可以设为默认模型 | `models-lifecycle.e2e-spec.ts` | Service 单元 | PASS |
| 5 | 停用和软删除关闭可选状态，并删除加密凭据 | `models-lifecycle.e2e-spec.ts` | Service 单元 | PASS |
| 6 | 自定义模型只允许安全公网 HTTPS 地址，拒绝私网和敏感配置 | `models-lifecycle.e2e-spec.ts` | Service 单元 | PASS |
| 7 | 模型列表和详情只返回凭据存在标记及脱敏提示 | `models-lifecycle.e2e-spec.ts` | Service 单元 | PASS |
| 8 | 新增发布/停用路由在数据库关闭时仍返回统一契约占位错误 | `api-contract.e2e-spec.ts` | API E2E | PASS |

## 覆盖率和已知缺口

覆盖率命令实际结果：语句 96.69%、分支 81.72%、函数 96.73%、行 97.43%。

已知缺口：当前 Controller 使用固定种子操作者，真实 JWT 身份、角色守卫、限流和资源授权留待认证阶段；本次没有实现真实 PostgreSQL 写入 E2E，事务行为通过 TypeORM DataSource Mock 验证。
