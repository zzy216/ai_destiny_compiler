# 发布文档状态一致性修正

- 日期：2026-07-19
- 状态：已完成
- 需求来源：用户要求“继续下一步”，阶段 8 已合并后进行发布候选文档收口。

## 一、需求说明

### 问题

阶段 8 已经通过 `merge: 合并阶段8 测试和发布` 合并到 `main`，但 MVP 执行清单仍有个别状态停留在“待合并到 `main`”。同时阶段 5～8 汇总把已交付的行动反馈、复盘、记忆管理和未交付的提醒自动化混在同一条部分完成项里，容易误读 MVP 内测候选范围。

### 预期结果

修正发布相关文档中的阶段状态，让 MVP 0.1 的已完成范围和后续增强项清晰分离。

### 验收标准

- [x] 阶段 8 分支状态明确为已合并到 `main`。
- [x] 行动反馈、复盘、记忆管理标记为已完成，提醒和 AI 自动化作为后续增强项单独说明。
- [x] 下一步决策说明更新为阶段 0～8 已完成，下一步进入内测发布执行或后续增强选择。
- [x] 文档格式检查通过。

### 非目标

- 不修改业务代码、测试代码或 OpenAPI 生成物。
- 不推送远程、不创建 PR、不执行真实部署。

### 假设和待确认问题

- 假设：当前 `main` 的最新合并提交 `813a213 merge: 合并阶段8 测试和发布` 是阶段 8 完成依据。
- 待确认：暂无；本次只修正文档状态。

## 二、当前状态

- 相关文件和符号：`docs/03.plan/02.MVP执行清单.md` — 阶段 8 表格仍写“待合并到 `main`”。
- 相关文件和符号：`docs/03.plan/01.MVP下一步实施计划.md` — 下一步决策仍只提到阶段 1～4 已完成。
- 相关文件和符号：`docs/05.analysis/2026-07-19-phase-7-action-review.md` — 明确阶段 7 已完成行动卡、执行反馈、复盘和记忆后端闭环，提醒调度仍是后续工作。
- 相关文件和符号：`docs/05.analysis/2026-07-19-phase-8-release.md` — 明确阶段 8 已完成，MVP 进入内测发布候选状态。
- 现有行为：文档读者需要同时查看 git 历史和分析记录才能还原真实状态。
- 现有测试或检查：阶段 8 已通过 `./scripts/verify-release.sh`；本次文档修正只需要格式和空白差异检查。

## 三、功能分析

### 范围和受影响组件

- `docs/03.plan/02.MVP执行清单.md` — 修正阶段表和阶段 5～8 汇总项。
- `docs/03.plan/01.MVP下一步实施计划.md` — 更新当前立即任务和下一步决策的状态说明。
- `docs/05.analysis/2026-07-19-release-docs-consistency.md` — 记录本次修正的依据和验证结果。

### 行为和数据流

本次不改变运行时行为，只改变文档对项目状态的表达。

### 边界条件和失败处理

- 若发现业务实现与文档状态冲突，只记录为后续工作，不在本次文档修正中扩大范围。
- 若格式检查失败，修正文档空白或 Markdown 结构后再提交。

### 风险和兼容性

- 风险：把未实现的提醒能力误标为已完成。缓解：将提醒调度、推送通知和 AI 自动化明确保留为后续增强项。

### 方案决策

采用最小文档修正，不新增阶段 9 或业务需求分支，避免把发布候选收口和后续产品增强混为一项工作。

## 四、实施计划

1. 更新 MVP 执行清单的阶段 8 合并状态和阶段 5～8 汇总项。
2. 更新 MVP 下一步实施计划中的当前状态和下一步决策。
3. 回填本分析记录的实施结果。
4. 执行 `git diff --check` 和文档关键词复查。

## 五、实施结果

### 已完成改动

更新 `docs/03.plan/02.MVP执行清单.md`，将阶段 8 状态从“待合并到 `main`”修正为“已合并到 `main`”，并把阶段 5～8 汇总拆成“行动执行反馈、复盘和记忆管理后端闭环已完成”与“提醒调度、推送通知和 AI 自动化复盘/记忆抽取为后续增强项”。更新 `docs/03.plan/01.MVP下一步实施计划.md`，明确阶段 1～8 已完成并合并到 `main`，下一步进入内测发布清单执行或新需求分支开发。

### 验证结果

- `git diff --check` — 通过
- `rg -n '待合并到 `main`' docs/03.plan` — 通过；计划文档中已无待合并状态

### 限制和后续工作

- 本次只修正文档状态，不执行远程推送、PR、部署、Apifox 导入或内测环境操作。
